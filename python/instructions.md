### Python Instructions

```bash
python3 main.py -h
    pygame 2.4.0 (SDL 2.26.4, Python 3.11.3)
    Hello from the pygame community. https://www.pygame.org/contribute.html
    usage: Boids Simulator [-h] [-c CONFIG]

    A Simulation of the movements of bird flocks using the Boids genetic algorithm

    options:
    -h, --help            show this help message and exit
    -c CONFIG, --config   Absolute path to default initial configuration
```